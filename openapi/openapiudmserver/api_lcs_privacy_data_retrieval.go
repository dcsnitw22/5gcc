/*
 * Nudm_SDM
 *
 * Nudm Subscriber Data Management Service.   © 2023, 3GPP Organizational Partners (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * API version: 2.2.3
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	//	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// LCSPrivacyDataRetrievalAPIController binds http requests to an api service and writes the service results to the http response
type LCSPrivacyDataRetrievalAPIController struct {
	service      LCSPrivacyDataRetrievalAPIServicer
	errorHandler ErrorHandler
}

// LCSPrivacyDataRetrievalAPIOption for how the controller is set up.
type LCSPrivacyDataRetrievalAPIOption func(*LCSPrivacyDataRetrievalAPIController)

// WithLCSPrivacyDataRetrievalAPIErrorHandler inject ErrorHandler into controller
func WithLCSPrivacyDataRetrievalAPIErrorHandler(h ErrorHandler) LCSPrivacyDataRetrievalAPIOption {
	return func(c *LCSPrivacyDataRetrievalAPIController) {
		c.errorHandler = h
	}
}

// NewLCSPrivacyDataRetrievalAPIController creates a default api controller
func NewLCSPrivacyDataRetrievalAPIController(s LCSPrivacyDataRetrievalAPIServicer, opts ...LCSPrivacyDataRetrievalAPIOption) Router {
	controller := &LCSPrivacyDataRetrievalAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the LCSPrivacyDataRetrievalAPIController
func (c *LCSPrivacyDataRetrievalAPIController) Routes() Routes {
	return Routes{
		"GetLcsPrivacyData": Route{
			strings.ToUpper("Get"),
			"/nudm-sdm/v2/{ueId}/lcs-privacy-data",
			c.GetLcsPrivacyData,
		},
	}
}

// GetLcsPrivacyData - retrieve a UE's LCS Privacy Subscription Data
func (c *LCSPrivacyDataRetrievalAPIController) GetLcsPrivacyData(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	query := r.URL.Query()
	ueIdParam := params["ueId"]
	if ueIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"ueId"}, nil)
		return
	}
	var supportedFeaturesParam string
	if query.Has("supported-features") {
		supportedFeaturesParam = query.Get("supported-features")
	}
	ifNoneMatchParam := r.Header.Get("If-None-Match")
	ifModifiedSinceParam := r.Header.Get("If-Modified-Since")
	result, err := c.service.GetLcsPrivacyData(r.Context(), ueIdParam, supportedFeaturesParam, ifNoneMatchParam, ifModifiedSinceParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
