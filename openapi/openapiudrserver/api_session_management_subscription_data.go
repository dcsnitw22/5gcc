/*
 * Nudr_DataRepository API OpenAPI file
 *
 * Unified Data Repository Service.   © 2023, 3GPP Organizational Partners (ARIB, ATIS, CCSA, ETSI, TSDSI, TTA, TTC).   All rights reserved.
 *
 * API version: 2.2.4
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"fmt"
	"net/http"
	"strconv"
	"strings"

	"github.com/gorilla/mux"
)

// SessionManagementSubscriptionDataAPIController binds http requests to an api service and writes the service results to the http response
type SessionManagementSubscriptionDataAPIController struct {
	service      SessionManagementSubscriptionDataAPIServicer
	errorHandler ErrorHandler
}

// SessionManagementSubscriptionDataAPIOption for how the controller is set up.
type SessionManagementSubscriptionDataAPIOption func(*SessionManagementSubscriptionDataAPIController)

// WithSessionManagementSubscriptionDataAPIErrorHandler inject ErrorHandler into controller
func WithSessionManagementSubscriptionDataAPIErrorHandler(h ErrorHandler) SessionManagementSubscriptionDataAPIOption {
	return func(c *SessionManagementSubscriptionDataAPIController) {
		c.errorHandler = h
	}
}

// NewSessionManagementSubscriptionDataAPIController creates a default api controller
func NewSessionManagementSubscriptionDataAPIController(s SessionManagementSubscriptionDataAPIServicer, opts ...SessionManagementSubscriptionDataAPIOption) Router {
	controller := &SessionManagementSubscriptionDataAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the SessionManagementSubscriptionDataAPIController
func (c *SessionManagementSubscriptionDataAPIController) Routes() Routes {
	return Routes{
		"QuerySmData": Route{
			strings.ToUpper("Get"),
			"/nudr-dr/v2/subscription-data/{ueId}/{servingPlmnId}/provisioned-data/sm-data",
			c.QuerySmData,
		},
	}
}

// QuerySmData - Retrieves the Session Management subscription data of a UE
func (c *SessionManagementSubscriptionDataAPIController) QuerySmData(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	query := r.URL.Query()
	ueIdParam := params["ueId"]
	fmt.Println("Request is coming from udr client to ude server")
	if ueIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"ueId"}, nil)
		return
	}
	fmt.Println("I am ueid")
	servingPlmnIdParam := params["servingPlmnId"]
	if servingPlmnIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"servingPlmnId"}, nil)
		return
	}
	fmt.Println("I am plmnidid")
	var singleNssaiParam Snssai
	if query.Has("single-nssai[sst]") {
		sNssaiSst := query.Get("single-nssai[sst]")
		var sNssaiSd string
		if query.Has("single-nssai[sd]") {
			sNssaiSd = query.Get("single-nssai[sd]")
		}
		sst, _ := strconv.Atoi(sNssaiSst)
		singleNssaiParam = Snssai{
			Sst: int32(sst),
			Sd:  sNssaiSd,
		}

	}
	var dnnParam string
	if query.Has("dnn") {
		dnnParam = query.Get("dnn")
	}
	var fieldsParam []string
	if query.Has("fields") {
		fieldsParam = strings.Split(query.Get("fields"), ",")
	}
	var supportedFeaturesParam string
	if query.Has("supported-features") {
		supportedFeaturesParam = query.Get("supported-features")
	}
	ifNoneMatchParam := r.Header.Get("If-None-Match")
	ifModifiedSinceParam := r.Header.Get("If-Modified-Since")
	result, err := c.service.QuerySmData(r.Context(), ueIdParam, servingPlmnIdParam, singleNssaiParam, dnnParam, fieldsParam, supportedFeaturesParam, ifNoneMatchParam, ifModifiedSinceParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
